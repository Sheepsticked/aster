; GENERATED by aster from aster.yaml — do not edit.
[aster-in-gsm1]                       ; carrier ingress (driver context)
exten => s,1,Set(__ASTER_MODEM=gsm1)
 same => n,Set(__ASTER_DID=)
 same => n,Set(CHANNEL(hangup_handler_push)=aster-hangup,s,1)
 same => n,Goto(aster-ring-gsm1,s,1)
exten => _[+0-9].,1,Set(__ASTER_MODEM=gsm1)
 same => n,Set(__ASTER_DID=${EXTEN})
 same => n,Set(CHANNEL(hangup_handler_push)=aster-hangup,s,1)
 same => n,Goto(aster-ring-gsm1,s,1)
exten => sms,1,System(/usr/local/bin/aster-emit sms gsm1 "${UNIQUEID}" "${BASE64_ENCODE(x${CALLERID(num)})}" "${SMS_BASE64}" "${BASE64_ENCODE(x${SMS_TS})}")
 same => n,ExecIf($["${SYSTEMSTATUS}"!="SUCCESS"]?Log(ERROR,aster-emit sms gsm1 failed: ${SYSTEMSTATUS}))
 same => n,Hangup()
exten => report,1,System(/usr/local/bin/aster-emit sms-report gsm1 "${UNIQUEID}" "${BASE64_ENCODE(x${SMS_REPORT_PAYLOAD})}" "${SMS_REPORT_TYPE}" "${SMS_REPORT_SUCCESS}" "${BASE64_ENCODE(x${SMS_REPORT_TS})}" "${BASE64_ENCODE(x${SMS_REPORT_DT})}" "${BASE64_ENCODE(x${SMS_REPORT})}")
 same => n,Hangup()
exten => ussd,1,Hangup()

[aster-ring-gsm1]                     ; generated from modems[].ring (used when incoming_context is null)
exten => s,1,Dial(PJSIP/504&PJSIP/505&PJSIP/506&PJSIP/507&PJSIP/508,120,mb(aster-jitterbuffer^s^1))
 same => n,Hangup()

[aster-out-gsm1]
exten => _+X.,1,ExecIf($["${PHONE_JITTERBUFFER}"!=""]?Set(JITTERBUFFER(${PHONE_JITTERBUFFER})=default))
 same => n,Dial(${GSM1}/${EXTEN},120)
 same => n,Hangup()
exten => _*X.,1,ExecIf($["${PHONE_JITTERBUFFER}"!=""]?Set(JITTERBUFFER(${PHONE_JITTERBUFFER})=default))
 same => n,Dial(${GSM1}/${EXTEN:1},120)
 same => n,Hangup()

[aster-phones-gsm1]                   ; endpoint context for phones with outbound: gsm1
include => internal
include => aster-out-gsm1

[aster-in-gsm2]                       ; carrier ingress (driver context)
exten => s,1,Set(__ASTER_MODEM=gsm2)
 same => n,Set(__ASTER_DID=)
 same => n,Set(CHANNEL(hangup_handler_push)=aster-hangup,s,1)
 same => n,Goto(aster-ring-gsm2,s,1)
exten => _[+0-9].,1,Set(__ASTER_MODEM=gsm2)
 same => n,Set(__ASTER_DID=${EXTEN})
 same => n,Set(CHANNEL(hangup_handler_push)=aster-hangup,s,1)
 same => n,Goto(aster-ring-gsm2,s,1)
exten => sms,1,System(/usr/local/bin/aster-emit sms gsm2 "${UNIQUEID}" "${BASE64_ENCODE(x${CALLERID(num)})}" "${SMS_BASE64}" "${BASE64_ENCODE(x${SMS_TS})}")
 same => n,ExecIf($["${SYSTEMSTATUS}"!="SUCCESS"]?Log(ERROR,aster-emit sms gsm2 failed: ${SYSTEMSTATUS}))
 same => n,Hangup()
exten => report,1,System(/usr/local/bin/aster-emit sms-report gsm2 "${UNIQUEID}" "${BASE64_ENCODE(x${SMS_REPORT_PAYLOAD})}" "${SMS_REPORT_TYPE}" "${SMS_REPORT_SUCCESS}" "${BASE64_ENCODE(x${SMS_REPORT_TS})}" "${BASE64_ENCODE(x${SMS_REPORT_DT})}" "${BASE64_ENCODE(x${SMS_REPORT})}")
 same => n,Hangup()
exten => ussd,1,Hangup()

[aster-ring-gsm2]                     ; generated from modems[].ring (used when incoming_context is null)
exten => s,1,Dial(PJSIP/511&PJSIP/512&PJSIP/513&PJSIP/514&PJSIP/515,120,mb(aster-jitterbuffer^s^1))
 same => n,Hangup()

[aster-out-gsm2]
exten => _+X.,1,ExecIf($["${PHONE_JITTERBUFFER}"!=""]?Set(JITTERBUFFER(${PHONE_JITTERBUFFER})=default))
 same => n,Dial(${GSM2}/${EXTEN},120)
 same => n,Hangup()
exten => _*X.,1,ExecIf($["${PHONE_JITTERBUFFER}"!=""]?Set(JITTERBUFFER(${PHONE_JITTERBUFFER})=default))
 same => n,Dial(${GSM2}/${EXTEN:1},120)
 same => n,Hangup()

[aster-phones-gsm2]                   ; endpoint context for phones with outbound: gsm2
include => internal
include => aster-out-gsm2

[aster-phones-internal]               ; endpoint context for phones with outbound: null
include => internal

[aster-jitterbuffer]
exten => s,1,ExecIf($["${PHONE_JITTERBUFFER}"!=""]?Set(JITTERBUFFER(${PHONE_JITTERBUFFER})=default))
 same => n,Return()

[aster-hangup]
exten => s,1,System(/usr/local/bin/aster-emit call-end "${ASTER_MODEM}" "${UNIQUEID}" "${BASE64_ENCODE(x${CALLERID(num)})}" "${BASE64_ENCODE(x${ASTER_DID})}" "${BASE64_ENCODE(x${DIALSTATUS})}" "${BASE64_ENCODE(x${ANSWEREDTIME})}" "${BASE64_ENCODE(x${CDR(disposition)})}" "${BASE64_ENCODE(x${HANGUPCAUSE})}" "${BASE64_ENCODE(x${DIALEDTIME})}")
 same => n,Return()
