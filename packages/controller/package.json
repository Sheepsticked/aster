{
  "name": "@aster/controller",
  "version": "0.0.0",
  "private": true,
  "description": "Aster controller — Fastify JSON API, node:sqlite store, AMI client, spool ingester, Telegram notification queue, operation runner and Asterisk configuration generator.",
  "type": "module",
  "engines": { "node": ">=24.15" },
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test",
    "lint": "tsc --noEmit -p ../../jsconfig.json"
  },
  "dependencies": {
    "fastify": "^5.12.3",
    "yaml": "^2.9.0"
  },
  "devDependencies": {
    "@types/node": "^24.13.4",
    "typescript": "^7.0.2"
  }
}
