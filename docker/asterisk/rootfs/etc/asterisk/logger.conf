; Aster — built-in defaults (see asterisk.conf): console only, so `docker logs` shows what happens.
; The production logger.conf adds `full => notice,warning,error,verbose(3)` on the logs volume.
[general]
dateformat = %F %T

[logfiles]
console => notice,warning,error,verbose(3)
