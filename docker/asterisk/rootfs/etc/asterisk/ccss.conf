; Aster — built-in defaults (see asterisk.conf): call completion (CCSS) at Asterisk defaults. The file only has to
; exist: without it Asterisk logs "Could not find valid ccss.conf file" as a WARNING on every boot.
