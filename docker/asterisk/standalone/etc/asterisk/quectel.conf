; Starter file (see asterisk.conf): no modems yet. Add a section per modem, for example [gsm1] with imei = <its IMEI>.
[general]
smsdb = /var/lib/asterisk/smsdb-quectel    ; one file per driver: both drivers run in one process

[defaults]
