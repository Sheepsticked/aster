; GENERATED by aster from aster.yaml — do not edit.
[aster-in-gsm_test]                   ; carrier ingress (driver context)
exten => s,1,Set(__ASTER_MODEM=gsm_test)
 same => n,Set(__ASTER_DID=)
 same => n,Set(CHANNEL(hangup_handler_push)=aster-hangup,s,1)
 same => n,Goto(aster-ring-gsm_test,s,1)
exten => _[+0-9].,1,Set(__ASTER_MODEM=gsm_test)
 same => n,Set(__ASTER_DID=${EXTEN})
 same => n,Set(CHANNEL(hangup_handler_push)=aster-hangup,s,1)
 same => n,Goto(aster-ring-gsm_test,s,1)
exten => sms,1,System(/usr/local/bin/aster-emit sms gsm_test "${UNIQUEID}" "${BASE64_ENCODE(x${CALLERID(num)})}" "${SMS_BASE64}" "${BASE64_ENCODE(x${SMS_TS})}")
 same => n,ExecIf($["${SYSTEMSTATUS}"!="SUCCESS"]?Log(ERROR,aster-emit sms gsm_test failed: ${SYSTEMSTATUS}))
 same => n,Hangup()
exten => report,1,System(/usr/local/bin/aster-emit sms-report gsm_test "${UNIQUEID}" "${BASE64_ENCODE(x${SMS_REPORT_PAYLOAD})}" "${SMS_REPORT_TYPE}" "${SMS_REPORT_SUCCESS}" "${BASE64_ENCODE(x${SMS_REPORT_TS})}" "${BASE64_ENCODE(x${SMS_REPORT_DT})}" "${BASE64_ENCODE(x${SMS_REPORT})}")
 same => n,Hangup()
exten => ussd,1,Hangup()

[aster-ring-gsm_test]                 ; generated from modems[].ring (used when incoming_context is null)
exten => s,1,Dial(PJSIP/599,120,mb(aster-jitterbuffer^s^1))
 same => n,Hangup()

[aster-out-gsm_test]
exten => _+X.,1,ExecIf($["${PHONE_JITTERBUFFER}"!=""]?Set(JITTERBUFFER(${PHONE_JITTERBUFFER})=default))
 same => n,GosubIf($["${ASTER_MODEM}"=""]?aster-outgoing,s,1(gsm_test,${EXTEN}))
 same => n,Dial(${GSM_TEST}/${EXTEN},120)
 same => n,Hangup()
exten => _*X.,1,ExecIf($["${PHONE_JITTERBUFFER}"!=""]?Set(JITTERBUFFER(${PHONE_JITTERBUFFER})=default))
 same => n,GosubIf($["${ASTER_MODEM}"=""]?aster-outgoing,s,1(gsm_test,${EXTEN:1}))
 same => n,Dial(${GSM_TEST}/${EXTEN:1},120)
 same => n,Hangup()

[aster-phones-gsm_test]               ; endpoint context for phones with outbound: gsm_test
include => internal
include => aster-out-gsm_test

[aster-in-gsm_dongle]                 ; carrier ingress (driver context)
exten => s,1,Set(__ASTER_MODEM=gsm_dongle)
 same => n,Set(__ASTER_DID=)
 same => n,Set(CHANNEL(hangup_handler_push)=aster-hangup,s,1)
 same => n,Goto(aster-ring-gsm_dongle,s,1)
exten => _[+0-9].,1,Set(__ASTER_MODEM=gsm_dongle)
 same => n,Set(__ASTER_DID=${EXTEN})
 same => n,Set(CHANNEL(hangup_handler_push)=aster-hangup,s,1)
 same => n,Goto(aster-ring-gsm_dongle,s,1)
exten => sms,1,System(/usr/local/bin/aster-emit sms gsm_dongle "${UNIQUEID}" "${BASE64_ENCODE(x${CALLERID(num)})}" "${SMS_BASE64}" "${BASE64_ENCODE(x${SMS_TS})}")
 same => n,ExecIf($["${SYSTEMSTATUS}"!="SUCCESS"]?Log(ERROR,aster-emit sms gsm_dongle failed: ${SYSTEMSTATUS}))
 same => n,Hangup()
exten => report,1,System(/usr/local/bin/aster-emit sms-report gsm_dongle "${UNIQUEID}" "${BASE64_ENCODE(x${SMS_REPORT_PAYLOAD})}" "${SMS_REPORT_TYPE}" "${SMS_REPORT_SUCCESS}" "${BASE64_ENCODE(x${SMS_REPORT_TS})}" "${BASE64_ENCODE(x${SMS_REPORT_DT})}" "${BASE64_ENCODE(x${SMS_REPORT})}")
 same => n,Hangup()
exten => ussd,1,Hangup()

[aster-ring-gsm_dongle]               ; generated from modems[].ring (used when incoming_context is null)
exten => s,1,Hangup()

[aster-out-gsm_dongle]
exten => _+X.,1,ExecIf($["${PHONE_JITTERBUFFER}"!=""]?Set(JITTERBUFFER(${PHONE_JITTERBUFFER})=default))
 same => n,GosubIf($["${ASTER_MODEM}"=""]?aster-outgoing,s,1(gsm_dongle,${EXTEN}))
 same => n,Dial(${GSM_DONGLE}/${EXTEN},120)
 same => n,Hangup()
exten => _*X.,1,ExecIf($["${PHONE_JITTERBUFFER}"!=""]?Set(JITTERBUFFER(${PHONE_JITTERBUFFER})=default))
 same => n,GosubIf($["${ASTER_MODEM}"=""]?aster-outgoing,s,1(gsm_dongle,${EXTEN:1}))
 same => n,Dial(${GSM_DONGLE}/${EXTEN:1},120)
 same => n,Hangup()

[aster-phones-gsm_dongle]             ; endpoint context for phones with outbound: gsm_dongle
include => internal
include => aster-out-gsm_dongle

[aster-in-gsm_uac]                    ; carrier ingress (driver context)
exten => s,1,Set(__ASTER_MODEM=gsm_uac)
 same => n,Set(__ASTER_DID=)
 same => n,Set(CHANNEL(hangup_handler_push)=aster-hangup,s,1)
 same => n,Goto(smoke-in,s,1)
exten => _[+0-9].,1,Set(__ASTER_MODEM=gsm_uac)
 same => n,Set(__ASTER_DID=${EXTEN})
 same => n,Set(CHANNEL(hangup_handler_push)=aster-hangup,s,1)
 same => n,Goto(smoke-in,s,1)
exten => sms,1,System(/usr/local/bin/aster-emit sms gsm_uac "${UNIQUEID}" "${BASE64_ENCODE(x${CALLERID(num)})}" "${SMS_BASE64}" "${BASE64_ENCODE(x${SMS_TS})}")
 same => n,ExecIf($["${SYSTEMSTATUS}"!="SUCCESS"]?Log(ERROR,aster-emit sms gsm_uac failed: ${SYSTEMSTATUS}))
 same => n,Hangup()
exten => report,1,System(/usr/local/bin/aster-emit sms-report gsm_uac "${UNIQUEID}" "${BASE64_ENCODE(x${SMS_REPORT_PAYLOAD})}" "${SMS_REPORT_TYPE}" "${SMS_REPORT_SUCCESS}" "${BASE64_ENCODE(x${SMS_REPORT_TS})}" "${BASE64_ENCODE(x${SMS_REPORT_DT})}" "${BASE64_ENCODE(x${SMS_REPORT})}")
 same => n,Hangup()
exten => ussd,1,Hangup()

[aster-out-gsm_uac]
exten => _+X.,1,ExecIf($["${PHONE_JITTERBUFFER}"!=""]?Set(JITTERBUFFER(${PHONE_JITTERBUFFER})=default))
 same => n,GosubIf($["${ASTER_MODEM}"=""]?aster-outgoing,s,1(gsm_uac,${EXTEN}))
 same => n,Dial(${GSM_UAC}/${EXTEN},120)
 same => n,Hangup()
exten => _*X.,1,ExecIf($["${PHONE_JITTERBUFFER}"!=""]?Set(JITTERBUFFER(${PHONE_JITTERBUFFER})=default))
 same => n,GosubIf($["${ASTER_MODEM}"=""]?aster-outgoing,s,1(gsm_uac,${EXTEN:1}))
 same => n,Dial(${GSM_UAC}/${EXTEN:1},120)
 same => n,Hangup()

[aster-phones-gsm_uac]                ; endpoint context for phones with outbound: gsm_uac
include => internal
include => aster-out-gsm_uac

[aster-in-gsm_unmapped]               ; carrier ingress (driver context)
exten => s,1,Set(__ASTER_MODEM=gsm_unmapped)
 same => n,Set(__ASTER_DID=)
 same => n,Set(CHANNEL(hangup_handler_push)=aster-hangup,s,1)
 same => n,Goto(aster-ring-gsm_unmapped,s,1)
exten => _[+0-9].,1,Set(__ASTER_MODEM=gsm_unmapped)
 same => n,Set(__ASTER_DID=${EXTEN})
 same => n,Set(CHANNEL(hangup_handler_push)=aster-hangup,s,1)
 same => n,Goto(aster-ring-gsm_unmapped,s,1)
exten => sms,1,System(/usr/local/bin/aster-emit sms gsm_unmapped "${UNIQUEID}" "${BASE64_ENCODE(x${CALLERID(num)})}" "${SMS_BASE64}" "${BASE64_ENCODE(x${SMS_TS})}")
 same => n,ExecIf($["${SYSTEMSTATUS}"!="SUCCESS"]?Log(ERROR,aster-emit sms gsm_unmapped failed: ${SYSTEMSTATUS}))
 same => n,Hangup()
exten => report,1,System(/usr/local/bin/aster-emit sms-report gsm_unmapped "${UNIQUEID}" "${BASE64_ENCODE(x${SMS_REPORT_PAYLOAD})}" "${SMS_REPORT_TYPE}" "${SMS_REPORT_SUCCESS}" "${BASE64_ENCODE(x${SMS_REPORT_TS})}" "${BASE64_ENCODE(x${SMS_REPORT_DT})}" "${BASE64_ENCODE(x${SMS_REPORT})}")
 same => n,Hangup()
exten => ussd,1,Hangup()

[aster-ring-gsm_unmapped]             ; generated from modems[].ring (used when incoming_context is null)
exten => s,1,Hangup()

[aster-out-gsm_unmapped]
exten => _+X.,1,ExecIf($["${PHONE_JITTERBUFFER}"!=""]?Set(JITTERBUFFER(${PHONE_JITTERBUFFER})=default))
 same => n,GosubIf($["${ASTER_MODEM}"=""]?aster-outgoing,s,1(gsm_unmapped,${EXTEN}))
 same => n,Dial(${GSM_UNMAPPED}/${EXTEN},120)
 same => n,Hangup()
exten => _*X.,1,ExecIf($["${PHONE_JITTERBUFFER}"!=""]?Set(JITTERBUFFER(${PHONE_JITTERBUFFER})=default))
 same => n,GosubIf($["${ASTER_MODEM}"=""]?aster-outgoing,s,1(gsm_unmapped,${EXTEN:1}))
 same => n,Dial(${GSM_UNMAPPED}/${EXTEN:1},120)
 same => n,Hangup()

[aster-phones-gsm_unmapped]           ; endpoint context for phones with outbound: gsm_unmapped
include => internal
include => aster-out-gsm_unmapped

[aster-in-gsm_ports]                  ; carrier ingress (driver context)
exten => s,1,Set(__ASTER_MODEM=gsm_ports)
 same => n,Set(__ASTER_DID=)
 same => n,Set(CHANNEL(hangup_handler_push)=aster-hangup,s,1)
 same => n,Goto(aster-ring-gsm_ports,s,1)
exten => _[+0-9].,1,Set(__ASTER_MODEM=gsm_ports)
 same => n,Set(__ASTER_DID=${EXTEN})
 same => n,Set(CHANNEL(hangup_handler_push)=aster-hangup,s,1)
 same => n,Goto(aster-ring-gsm_ports,s,1)
exten => sms,1,System(/usr/local/bin/aster-emit sms gsm_ports "${UNIQUEID}" "${BASE64_ENCODE(x${CALLERID(num)})}" "${SMS_BASE64}" "${BASE64_ENCODE(x${SMS_TS})}")
 same => n,ExecIf($["${SYSTEMSTATUS}"!="SUCCESS"]?Log(ERROR,aster-emit sms gsm_ports failed: ${SYSTEMSTATUS}))
 same => n,Hangup()
exten => report,1,System(/usr/local/bin/aster-emit sms-report gsm_ports "${UNIQUEID}" "${BASE64_ENCODE(x${SMS_REPORT_PAYLOAD})}" "${SMS_REPORT_TYPE}" "${SMS_REPORT_SUCCESS}" "${BASE64_ENCODE(x${SMS_REPORT_TS})}" "${BASE64_ENCODE(x${SMS_REPORT_DT})}" "${BASE64_ENCODE(x${SMS_REPORT})}")
 same => n,Hangup()
exten => ussd,1,Hangup()

[aster-ring-gsm_ports]                ; generated from modems[].ring (used when incoming_context is null)
exten => s,1,Hangup()

[aster-out-gsm_ports]
exten => _+X.,1,ExecIf($["${PHONE_JITTERBUFFER}"!=""]?Set(JITTERBUFFER(${PHONE_JITTERBUFFER})=default))
 same => n,GosubIf($["${ASTER_MODEM}"=""]?aster-outgoing,s,1(gsm_ports,${EXTEN}))
 same => n,Dial(${GSM_PORTS}/${EXTEN},120)
 same => n,Hangup()
exten => _*X.,1,ExecIf($["${PHONE_JITTERBUFFER}"!=""]?Set(JITTERBUFFER(${PHONE_JITTERBUFFER})=default))
 same => n,GosubIf($["${ASTER_MODEM}"=""]?aster-outgoing,s,1(gsm_ports,${EXTEN:1}))
 same => n,Dial(${GSM_PORTS}/${EXTEN:1},120)
 same => n,Hangup()

[aster-phones-gsm_ports]              ; endpoint context for phones with outbound: gsm_ports
include => internal
include => aster-out-gsm_ports

[aster-phones-internal]               ; endpoint context for phones with outbound: null
include => internal

[aster-jitterbuffer]
exten => s,1,ExecIf($["${PHONE_JITTERBUFFER}"!=""]?Set(JITTERBUFFER(${PHONE_JITTERBUFFER})=default))
 same => n,Return()

[aster-outgoing]
exten => s,1,Set(ASTER_MODEM=${ARG1})
 same => n,Set(ASTER_DID=${ARG2})
 same => n,Set(ASTER_DIRECTION=out)
 same => n,Set(CHANNEL(hangup_handler_push)=aster-hangup,s,1)
 same => n,Return()

[aster-hangup]
exten => s,1,System(/usr/local/bin/aster-emit call-end "${ASTER_MODEM}" "${UNIQUEID}" "${BASE64_ENCODE(x${CALLERID(num)})}" "${BASE64_ENCODE(x${ASTER_DID})}" "${BASE64_ENCODE(x${DIALSTATUS})}" "${BASE64_ENCODE(x${ANSWEREDTIME})}" "${BASE64_ENCODE(x${CDR(disposition)})}" "${BASE64_ENCODE(x${HANGUPCAUSE})}" "${BASE64_ENCODE(x${DIALEDTIME})}" "${ASTER_DIRECTION}")
 same => n,Return()
