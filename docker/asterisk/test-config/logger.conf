; Aster — test-config (see asterisk.conf): console (docker logs, read by smoke-test.sh) plus the production `full`
; log file on /var/log/asterisk, which the apply check scans.
[general]
dateformat = %F %T

[logfiles]
console => notice,warning,error,verbose(3)
full => notice,warning,error,verbose(3)
