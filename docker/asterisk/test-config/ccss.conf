; Aster — test-config copy of the built-in default (see asterisk.conf): call completion (CCSS) at Asterisk defaults.
; The file only has to exist: without it Asterisk logs "Could not find valid ccss.conf file" as a WARNING on every boot.
