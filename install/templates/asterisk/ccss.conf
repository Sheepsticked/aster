; Aster — starter file (hand-owned, installed only if absent). Applying it restarts Asterisk: CCSS cannot reload.
; Call completion at Asterisk defaults. The file only has to exist: without it Asterisk logs
; "Could not find valid ccss.conf file" as a WARNING on every boot.
