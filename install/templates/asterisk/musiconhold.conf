; Aster — starter file (hand-owned, installed only if absent). Reload: moh reload.
; The files are the ones shipped in the image under astdatadir (/usr/share/asterisk/moh).
[default]
mode = files
directory = moh
