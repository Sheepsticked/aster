; Aster — starter file (hand-owned, installed only if absent). Reload: module reload res_rtp_asterisk.so.
; 200 ports is enough for the two modems and their phones; the range must be open on the LAN firewall, if there is one.
[general]
rtpstart = 10000
rtpend = 10200
