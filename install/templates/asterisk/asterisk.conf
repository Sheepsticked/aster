; Aster — starter file (hand-owned, installed only if absent). Applying it under Config restarts Asterisk.
; Container paths; keep in step with the image's default (docker/asterisk/rootfs/etc/asterisk/asterisk.conf).
[directories]
astetcdir => /etc/asterisk
astmoddir => /usr/lib/asterisk/modules
astvarlibdir => /var/lib/asterisk
astdbdir => /var/lib/asterisk
astkeydir => /var/lib/asterisk
astdatadir => /usr/share/asterisk
astagidir => /usr/share/asterisk/agi-bin
astspooldir => /var/spool/asterisk
astrundir => /var/run/asterisk
astlogdir => /var/log/asterisk
astsbindir => /usr/sbin

[options]
; Console (what Docker logs) at notice and above: verbose lines go only to logs/asterisk/full (logger.conf).
; `aster console` (asterisk -rvvv) raises the verbosity of that session alone.
verbose = 0
debug = 0
execincludes = no          ; #exec in config files is never allowed (the controller's lint rejects it as well)
hideconnect = yes          ; keep `asterisk -rx` healthchecks out of the log
transmit_silence = no
nocolor = yes              ; plain text in `docker logs`
timestamp = yes            ; timestamps on verbose lines
