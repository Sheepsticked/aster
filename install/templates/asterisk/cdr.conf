; Aster — starter file (hand-owned, installed only if absent). Reload: module reload cdr.
; The CDR core is on because the hangup handler reads CDR(disposition) to tell a missed call from an answered one;
; no CDR backend is built into the image, so nothing is written anywhere.
[general]
enable = yes
