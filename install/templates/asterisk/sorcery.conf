; Aster — starter file (hand-owned, installed only if absent). Applied at an Asterisk restart.
; Phone registrations in memory, not on the SD card: after a restart a phone is reachable again once it re-registers.
[res_pjsip]
contact=memory
